<% @page_title = "#{@user.name}'s Photo Albums" %>
<h1><%= @page_title %></h1>
<ul>
  <% for album in @albums %>
  <li><%= link_to(album.caption, :user => params[:user], :album => album.permalink, :action => 'show') %></li>
  <% end %>
</ul>